info:
  name: Login
  type: http
  seq: 2

http:
  method: POST
  url: "{{BASE_URL}}/login"
  body:
    type: json
    data: |-
      {
          "email": "ayalasanchezcristian2000@gmail.com",
          "password": "nuevapassword123"
      }
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
