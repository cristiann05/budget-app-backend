info:
  name: Resend-email
  type: http
  seq: 4

http:
  method: POST
  url: "{{BASE_URL}}/email/resend"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
