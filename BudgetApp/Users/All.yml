info:
  name: All
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/users"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
