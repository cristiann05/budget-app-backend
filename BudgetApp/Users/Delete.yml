info:
  name: Delete
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{BASE_URL}}/users/1"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
