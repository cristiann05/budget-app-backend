info:
  name: Create
  type: http
  seq: 2

http:
  method: POST
  url: "{{BASE_URL}}/budgets"
  body:
    type: json
    data: |-
      {
          "total_amount": 2000.00,
          "month": "2026-04-01",
          "is_recurring": false
      }
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
