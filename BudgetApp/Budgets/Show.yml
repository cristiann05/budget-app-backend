info:
  name: Show
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE_URL}}/budgets/1"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
