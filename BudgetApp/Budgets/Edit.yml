info:
  name: Edit
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{BASE_URL}}/budgets/1"
  body:
    type: json
    data: |-
      {
          "total_amount": 2500.00,
          "is_recurring": true
      }
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
